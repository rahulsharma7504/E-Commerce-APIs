
const {User_model} =require('../Model/userModel')

const register= async(req,res)=>{
try {
    
    
} catch (error) {
    if(error) throw error
    res.status.send("An Error Occred")
    
}
}

module.exports={
    register
}